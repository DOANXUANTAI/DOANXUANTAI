using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class PathFinder 
{


   /*a public List<overlayTile> Findpath  (overlayTile start , overlayTile End)

    {

        List<overlayTile> openList = new List<overlayTile>();
        List<overlayTile> closeList = new List<overlayTile>();
        openList.Add(start);
        while(openList.Count > 0)
        {

            overlayTile CurrentoverlayTile = openList.OrderBy(x=> x.F).First();
            openList.Remove(CurrentoverlayTile);
            closeList.Add(CurrentoverlayTile);

            if(CurrentoverlayTile == End)
            {


            }

            var neighbourTiles = GetHeigbourTiles(CurrentoverlayTile);

        }

    }
   a*/
    
}
